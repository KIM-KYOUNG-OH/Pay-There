spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/payThere?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: ${SECRET_USERNAME}
    password: ${SECRET_PASSWORD}
